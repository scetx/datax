// instantiating the variables to constant values
export const COLORS = {
    [-1]: "white",
    0: "green",
    1: "red",
    // map of the nodes by color defined by state
};

export const CLASSES = {
    [-1]: "resistant",
    0: "normal",
    1: "compromised"
};

// setting the numbers of the simulation
export const BOX_SIZE = 400; // width and height of box
export const HISTORY = 300;  // length of history array
export const NODES = 300;  // number of nodes
export const SIMULATOR_SIZE = 500;  // size of simulator box in pixels
